version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.7.3

    steps:
      - checkout

      - run:
          name: install dependencies
          command: |
            python3 -m venv venv
            . venv/bin/activate
            pip install -r encouragemint/requirements.txt
            sudo pip install pylint
      - run:
          name: run pylint
          command: pylint encouragemint/*.py --disable=E0401,C0103

      - run:
          name: run tests
          command: |
            . venv/bin/activate
            mkdir -p test_results
            python manage.py test --settings=encouragemint.settings.local --cover-html --with-xunit --xunit-file=test_results/test_result.xml

      - store_artifacts:
          path: encouragemint/test_results
          destination: test

      - store_test_results:
          path: encouragemint/test_results